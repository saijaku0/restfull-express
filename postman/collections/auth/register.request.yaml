$kind: http-request
url: "{{base_api}}/api/auth/register"
method: POST
body:
  type: json
  content: "{\r

    \  \"email\": \"test@test.com\",\r

    \  \"password\": \"password123\",\r

    \  \"firstName\": \"Ivan\",\r

    \  \"lastName\": \"Petrov\"\r

    }"
scripts:
  - type: afterResponse
    code: "if (pm.response.code === 200 || pm.response.code === 201) {\r

      \  const body = pm.response.json();\r

      \  if (body.accessToken) {\r

      \    pm.environment.set('accessToken', body.accessToken);\r

      \    console.log('accessToken saved to environment');\r

      \  }\r

      }"
    language: text/javascript
order: 2000
